<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Project extends Model
{
    //
//  protected $table = 'projects';
     /**
     * モデルに関連付けるデータベースのテーブルを指定
     *
     * @var string
     */
     //protected $table = 'projects';

     /**
     * createメソッド利用時に、入力を受け付けないカラムの指定
     *
     * @var array
     */
     //protected $fillable = array('title', 'description'); // この行を追加。
}
